def countdown(n):
    if n == 0:
        print("Done!")
    else:
        print(n)
        countdown(n - 1)
countdown(5)

"""
   In the above code, I have add one more = to solve the error. single = is used to store the value in variable. 
   and double == is used to compare the two value.
   """
